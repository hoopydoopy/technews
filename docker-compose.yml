version: "3.8"

services:
  frontend:
    image: technews-frontend
    build: 
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    command: ["npm", "run", "dev", "--", "--host"]
